#!/bin/bash

case "$1" in
    PHP_CodeSniffer)
        ./vendor/bin/phpcs
        exit $?
        ;;
    PHPUnit)
        cd ${TESTING_SITE_DIR}/web
        ../vendor/bin/phpunit
        exit $?
        ;;
    *)
        echo "Unknown test '$1'"
        exit 1
esac
